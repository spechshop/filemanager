const https=require('https');const [port,seconds0,rate0]=process.argv.slice(2),seconds=Number(seconds0),rate=Number(rate0);let count=0,errors=0;const lat=[];const agent=new https.Agent({keepAlive:true,maxSockets:16,rejectUnauthorized:false});
function send(){const t=performance.now();https.get({host:'127.0.0.1',port,path:'/',agent,headers:{'Accept-Encoding':'gzip'}},res=>{if(res.statusCode!==200)errors++;res.resume();res.on('end',()=>{count++;lat.push(performance.now()-t)})}).on('error',()=>errors++)}
const timer=setInterval(send,1000/rate);setTimeout(()=>{clearInterval(timer);setTimeout(()=>{agent.destroy();lat.sort((a,b)=>a-b);console.log(JSON.stringify({rate,seconds,count,errors,p50:lat[Math.floor(lat.length*.5)],p95:lat[Math.floor(lat.length*.95)],p99:lat[Math.floor(lat.length*.99)]}))},1000)},seconds*1000)
